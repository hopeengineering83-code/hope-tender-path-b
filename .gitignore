node_modules
.next
.vercel
.env
.env.local
.env.production
*.log
.claude/
*.tsbuildinfo

# PR #256 — local-only benchmark samples used for gap analysis.
# Contains sensitive tender PDFs and generated proposals; never
# commit to git.
benchmark-samples/

# Local dev file storage (storage adapter writes here in non-Vercel environments)
.storage/
