name: Retired audited gap patch

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The audited gap patch has already been applied. Use the standard CI workflow for validation."
