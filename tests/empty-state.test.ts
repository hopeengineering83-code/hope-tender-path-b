// EmptyState component was deleted as dead code.
// This test file is retained as a placeholder to avoid import errors.
import { describe, it } from "node:test";

describe("EmptyState component (deleted)", () => {
  it.skip("component was deleted as dead code — no assertions needed", () => {});
});
