// ExtractionQualityDashboard component was deleted as dead code.
// Extraction quality is now handled by ExtractionQualityPanel inside
// a collapsed Disclosure on the tender detail page.
import { describe, it } from "node:test";

describe("ExtractionQualityDashboard (deleted)", () => {
  it.skip("component was deleted as dead code — no assertions needed", () => {});
});
