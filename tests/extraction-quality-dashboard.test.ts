// ExtractionQualityDashboard component was deleted as dead code.
// Extraction quality is now handled by ExtractionQualityPanel.
import { describe, it } from "node:test";

describe("ExtractionQualityDashboard (deleted)", () => {
  it.skip("component was deleted as dead code", () => {});
});
