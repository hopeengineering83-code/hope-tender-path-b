{
  "framework": "nextjs",
  "buildCommand": "node scripts/check-env.mjs && prisma generate && prisma db push --accept-data-loss=false && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/ai-jobs/run-next",
      "schedule": "0 0 * * *"
    },
    {
      "path": "/api/cron/cleanup-old-records",
      "schedule": "0 3 * * 0"
    }
  ]
}
