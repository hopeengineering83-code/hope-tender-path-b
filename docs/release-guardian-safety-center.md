# Release Guardian

The Release Guardian is an admin-only, fail-closed safety center. It checks configuration overrides, database promotion schema, recent AI Analyze failures, storage/session readiness, and release blockers. It never merges, deploys, changes secrets, invents tender facts, or unlocks generation/export.