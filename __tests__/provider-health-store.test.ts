// Provider health store test — see tests/provider-health-store.test.ts for the
// full test suite (that file is picked up by the npm test runner).
// This file mirrors the same tests so Jest-based runners (if added later) also
// find them in __tests__/ as per the task specification.

export { } from "../tests/provider-health-store.test.ts";
